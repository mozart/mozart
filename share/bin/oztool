#! /bin/sh
#
# oztool c++ ...        invokes the right c++ compiler with the right options
# oztool cc  ...        same for the c compiler
# oztool ld  ...        creates DLLs

: ${OZHOME="/usr/local/oz"}
: ${OZPLATFORM="$OZHOME/bin/ozplatform"}
: ${PLATFORM=`$OZPLATFORM`}
: ${OZTOOL="$OZHOME/platform/$PLATFORM/oztool.sh"}

# invoke a platform specific script

exec "$OZTOOL" "$@"
