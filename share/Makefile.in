BUILDTOP=@BUILDTOP@
TOPDIR = @TOPDIR@
PREFIX = @prefix@
RM     = @RM@ -rf

SUBDIRS = \
	bin lib tools test demo elisp examples


.PHONY:	bootstrap all install clean distclean realclean veryclean World files check

bootstrap::

bootstrap all install clean realclean veryclean depend distclean::
	dirs="$(SUBDIRS)"; for i in $$dirs; do (cd $$i && $(MAKE) $@); done

clean realclean veryclean distclean::
	-$(RM) *~

distclean:: 
	-$(RM) Makefile
	-$(RM) config.log config.status config.cache

# need to put ozdynld in the PATH for the link test
check:
	PATH="$$PATH:$(TOPDIR)/share/bin" && cd test && $(MAKE) boot-check
