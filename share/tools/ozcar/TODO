-- write mail to changes@mozart-oz.org

-- query dialog for remote environments

-- improve reflection, procedure print names?
-- reflection of remote values:  Reflect them as the value the
   user sees in the environment display (short form/print depth-width)

-- debugging within spaces
