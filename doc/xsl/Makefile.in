PREFIX          = @prefix@
VPATH           = @srcdir@
SHDOCDIR        = $(PREFIX)/share/doc
DOCDIR          = $(PREFIX)/doc
BUILDDIR        = @BUILDDIR@

INSTALL         = @INSTALL@
INSTALL_FILE    = $(INSTALL) -m 444
INSTALL_DIR     = @INSTALL_DIR@
HOMEURL         = @HOMEURL@

.PHONY: all quick bootstrap check install clean veryclean distclean depend bootquick

all bootstrap:: TeX.class

bootquick::

%.class: %.java
        javac -d . $<

Makefile: Makefile.in ../config.status
        cd .. && ./config.status

veryclean clean::
        -rm -f *~ TeX.class

distclean:: veryclean
        -rm -f Makefile
