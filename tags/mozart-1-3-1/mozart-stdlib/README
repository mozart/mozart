To install the Mozart Standard Library, you should have Mozart already
installed on your system.  Assuming Mozart is installed in $PREFIX, you
should invoke:

	$PREFIX/bin/ozengine ./ozmake.ozf --prefix=$PREFIX --install --package=mozart-stdlib.pkg

If a version of the Mozart Standard Library is already installed, you
should invoke instead:

	$PREFIX/bin/ozengine ./ozmake.ozf --prefix=$PREFIX --upgrade --package=mozart-stdlib.pkg

If you do not have sufficient privileges to install the Mozart Standard
Library in the global Mozart installation directory, you can instead
install it in your private Oz area (i.e. $HOME/.oz); however, you will be
the only one to benefit from this.  Invoke:

	$PREFIX/bin/ozengine ./ozmake.ozf --install --package=mozart-stdlib.pkg
or	$PREFIX/bin/ozengine ./ozmake.ozf --upgrade --package=mozart-stdlib.pkg
